{
    "tasks": [
        {
            "type": "shell",
            "label": "clang++ build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "${workspaceFolder}/GridSearch/src/**.cpp",
                "-I${workspaceFolder}/GridSearch/src",
                "-o",
                "${workspaceFolder}/bin/vscode/GridSearch/GridSearch.out",
                "-std=c++17"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}